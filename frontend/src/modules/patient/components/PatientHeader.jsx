/**
 * PatientHeader.jsx – Patient Module
 *
 * Top navigation/header shown during the patient intake flow.
 * Displays progress steps, selected language, and kiosk branding.
 *
 * Developer 1 owns this file.
 */

import React from "react";

function PatientHeader({ currentStep, totalSteps }) {
  // TODO: Implement patient flow header with step indicator
  return (
    <header>
      {/* Implement: logo, step counter, language indicator */}
    </header>
  );
}

export default PatientHeader;
