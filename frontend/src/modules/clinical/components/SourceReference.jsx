/** SourceReference.jsx – Clinical Module – Dev 4 */
import React from "react";
function SourceReference({ source }) {
  return <span>{/* AI | conversation | document — with icon */}</span>;
}
export default SourceReference;
