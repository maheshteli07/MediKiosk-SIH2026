/** TimelineEvent.jsx – Clinical Module – Dev 4 */
import React from "react";
function TimelineEvent({ event }) {
  return <div>{/* date, event title, description, source */}</div>;
}
export default TimelineEvent;
